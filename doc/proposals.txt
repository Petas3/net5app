Proposals

Processing of groups can start as soon as group reading is completed in a different thread.
Application can be further extended to enable more input/output data formats or DB connectors.
As the app is written in .NET Core it can be deployed as web based app easily. Docker deployment for scaling.
Charting and GUI can be created easily as .NET contains necessary charting components.